using EventsService as service from '../../srv/events-service';

annotate service.Events with @(
    UI.FieldGroup #GeneratedGroup :{
        $Type :' UI.FieldGropuType',
        Data : [
            
        ]
    }
);

